# FACTOR LIST=

def factor_list(y):
    if y <= 0:
      return False
    else:
      i = 1
      while i <= y:
        if y % i == 0:
           list_factor.append(i)
        i += 1
      return list_factor

y = int(input("Give me a factor number bigger than '0'"))
list_factor= []
factor_list(y)
