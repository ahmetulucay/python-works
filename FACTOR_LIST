# FACTOR LIST=

def factor_list(y):
    if y <= 0:
        return False
    else:
        a = 1
        while a <= y:
            if y % a == 0:
                list_factor.append(a)
            a += 1
        return list_factor

y = int(input("Give me a factor number bigger than '0'"))
list_factor= []
factor_list(y)
